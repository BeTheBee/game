export function showTown(container, switchRoom) {
  const title = document.createElement('h1');
  title.textContent = 'Welcome to Town';

  container.appendChild(title);
  // Add town UI here...
}